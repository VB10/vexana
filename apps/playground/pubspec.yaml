name: playground
description: vexana canlı demo — Flutter Web. Yayınlanmaz.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  vexana:
    path: ../../packages/vexana

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
