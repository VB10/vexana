name: benchmark
description: vexana performans ölçümleri. Yayınlanmaz.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  vexana:
    path: ../packages/vexana

dev_dependencies:
  flutter_test:
    sdk: flutter
